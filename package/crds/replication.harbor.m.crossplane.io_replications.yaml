---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: replications.replication.harbor.m.crossplane.io
spec:
  group: replication.harbor.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harbor
    kind: Replication
    listKind: ReplicationList
    plural: replications
    singular: replication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .spec.forProvider.name
      name: POLICY
      type: string
    - jsonPath: .spec.forProvider.destinationReg.name
      name: DESTINATION
      type: string
    - jsonPath: .spec.forProvider.trigger
      name: TRIGGER
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A Replication is a managed resource that represents a Harbor
          replication policy for cross-registry synchronization.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A ReplicationSpec defines the desired state of a Replication
              policy.
            properties:
              forProvider:
                description: ReplicationParameters defines the desired state of a
                  Replication policy
                properties:
                  deleteSourceTag:
                    description: DeleteSourceTag removes source image tags after replication
                    type: boolean
                  description:
                    description: Description of the replication policy
                    type: string
                  destinationReg:
                    description: DestinationReg is the destination registry configuration
                    properties:
                      name:
                        description: Name is the destination registry name
                        type: string
                      namespace:
                        description: Namespace is the namespace in destination registry
                        type: string
                      url:
                        description: URL is the destination registry URL
                        type: string
                    required:
                    - name
                    type: object
                  enabled:
                    default: true
                    description: Enabled controls if the policy is active
                    type: boolean
                  filters:
                    description: Filters define which repositories/tags to replicate
                    items:
                      description: ReplicationFilter defines filter rules for replication
                      properties:
                        type:
                          description: 'Type is the filter type: repository, tag,
                            label, resource'
                          enum:
                          - repository
                          - tag
                          - label
                          - resource
                          type: string
                        value:
                          description: Value is the filter value
                          type: string
                      required:
                      - type
                      - value
                      type: object
                    type: array
                  name:
                    description: Name is the name of the replication policy
                    type: string
                  override:
                    default: true
                    description: Override overwrites images in destination
                    type: boolean
                  sourceRegistry:
                    description: SourceRegistry is the source registry name (optional
                      for local registry)
                    type: string
                  trigger:
                    description: 'Trigger is the replication trigger: manual, scheduled,
                      event_based'
                    enum:
                    - manual
                    - scheduled
                    - event_based
                    type: string
                required:
                - destinationReg
                - filters
                - name
                - trigger
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A ReplicationStatus represents the observed state of a Replication
              policy.
            properties:
              atProvider:
                description: ReplicationObservation defines the observed state of
                  a Replication policy
                properties:
                  creationTime:
                    description: CreationTime is when the policy was created
                    format: date-time
                    type: string
                  enabled:
                    description: Enabled indicates if the policy is currently active
                    type: boolean
                  id:
                    description: ID is the unique identifier of the replication policy
                    type: string
                  lastExecutionStatus:
                    description: LastExecutionStatus is the status of the last execution
                    type: string
                  updateTime:
                    description: UpdateTime is when the policy was last updated
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
