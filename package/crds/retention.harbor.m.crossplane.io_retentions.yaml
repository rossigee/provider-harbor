---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: retentions.retention.harbor.m.crossplane.io
spec:
  group: harbor.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harbor
    kind: Retention
    listKind: RetentionList
    plural: retentions
    singular: retention
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .spec.forProvider.projectId
      name: PROJECT
      type: string
    - jsonPath: .spec.forProvider.rules | length
      name: RULES
      type: integer
    - jsonPath: .spec.forProvider.trigger
      name: TRIGGER
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A Retention is a managed resource that represents a Harbor retention
          policy for automatic image cleanup.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A RetentionSpec defines the desired state of a Retention
              policy.
            properties:
              forProvider:
                description: RetentionParameters defines the desired state of a Retention
                  policy
                properties:
                  description:
                    description: Description of the retention policy
                    type: string
                  enabled:
                    default: true
                    description: Enabled controls if the policy is active
                    type: boolean
                  projectId:
                    description: ProjectID is the ID of the project
                    type: string
                  rules:
                    description: Rules define the cleanup rules
                    items:
                      description: RetentionRule defines a retention rule
                      properties:
                        parameters:
                          additionalProperties:
                            type: string
                          description: 'Parameters are rule-specific parameters (e.g.,
                            {"k": "10"})'
                          type: object
                        ruleType:
                          description: 'RuleType: always, latestPushedK, latestPulledN'
                          enum:
                          - always
                          - latestPushedK
                          - latestPulledN
                          - daysSinceLastPull
                          - daysSinceLastPush
                          type: string
                        tagSelectors:
                          description: TagSelectors define which tags to apply this
                            rule to
                          items:
                            type: string
                          type: array
                      required:
                      - ruleType
                      type: object
                    minItems: 1
                    type: array
                  trigger:
                    description: 'Trigger: manual, scheduled'
                    enum:
                    - manual
                    - scheduled
                    type: string
                required:
                - projectId
                - rules
                - trigger
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A RetentionStatus represents the observed state of a Retention
              policy.
            properties:
              atProvider:
                description: RetentionObservation defines the observed state of a
                  Retention policy
                properties:
                  creationTime:
                    description: CreationTime is when the policy was created
                    format: date-time
                    type: string
                  enabled:
                    description: Enabled indicates if the policy is active
                    type: boolean
                  id:
                    description: ID is the unique identifier of the retention policy
                    type: string
                  lastExecutionTime:
                    description: LastExecutionTime of the retention cleanup
                    format: date-time
                    type: string
                  updateTime:
                    description: UpdateTime is when the policy was last updated
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
